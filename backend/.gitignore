# All terraform state/lock file
*/terraform.tfvars
*/terraform.tfstate.backup
*/terraform.tfstate
*/.terraform.lock.hcl
*/.terraform

# tfstate and application infra for developer account
/infra-dev
/tfstate-infra-dev

# backend needs to be modified for dev and prod so will gitignore for now
*/backend.tf

# Deployment folder containing binary and zip
**/deploy

**/postman_*.json
