{
    "type": "object",
    "required": [
        "organizationName"
    ],
    "properties": {
        "organizationName": {
            "type": "string",
            "minLength": 4,
            "maxLength": 32,
            "pattern": "^(?! )(?!.*  )[A-Za-z0-9']+(?: [A-Za-z0-9']+)*$",
            "description": "Organization name must be 4-32 characters, alphanumeric and apostrophe only, cannot start with space or have consecutive spaces."
        },
        "description": {
            "type": "string",
            "maxLength": 300,
            "pattern": "^(?!\\s)(?!.*  ).*\\S$",
            "description": "Optional. Up to 300 characters. Cannot start with whitespace, have consecutive spaces, or end with whitespace."
        }
    }
}