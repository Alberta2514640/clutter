export type PaletteItem = {
  label: string;
  badge: string;
  category: "GENERAL" | "STORAGE" | "COMPUTE" | "NETWORK";
};

export type NodeData = {
  label: string;
  badge: string;
  category: string;
};
