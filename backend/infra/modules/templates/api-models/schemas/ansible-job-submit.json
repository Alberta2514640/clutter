{
  "type": "object",
  "required": [
    "target_instance_ids",
    "playbook_s3_key"
  ],
  "properties": {
    "config_id": {
      "type": "string",
      "description": "Optional configuration ID for the job"
    },
    "target_instance_ids": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^i-[0-9a-fA-F]{8,17}$"
      },
      "minItems": 1,
      "description": "List of EC2 instance IDs to target"
    },
    "playbook_s3_key": {
      "type": "string",
      "minLength": 1,
      "maxLength": 512,
      "description": "S3 key for the playbook file"
    },
    "extra_vars": {
      "type": "object",
      "additionalProperties": true,
      "description": "Optional extra variables for the playbook"
    }
  },
  "additionalProperties": false
}
